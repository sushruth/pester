query SearchPackage($query: String!) {
	searchPackage(query: $query) {
		name
		version
		description
		score {
			final
			detail {
				quality
				popularity
				maintenance
			}
		}
		searchScore
	}
}
